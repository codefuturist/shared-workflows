name: Setup Node.js + pnpm
description: Checkout, enable pnpm via corepack, configure Node.js with caching, and install dependencies

inputs:
  node-version:
    description: Node.js version to install
    required: false
    default: "24"
  registry-url:
    description: npm registry URL (set for publishing)
    required: false
    default: ""
  fetch-depth:
    description: Git fetch depth (0 for full history)
    required: false
    default: "1"

runs:
  using: composite
  steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: ${{ inputs.fetch-depth }}

    # v6 reads pnpm version from package.json `packageManager` cleanly.
    # v4 errored when `packageManager` was also present in package.json
    # ("Multiple versions of pnpm specified") because v4 carried its own
    # default of "9" that conflicted with the explicit pin.
    - uses: pnpm/action-setup@v6

    - uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm
        registry-url: ${{ inputs.registry-url || '' }}

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
